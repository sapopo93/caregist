# Pipeline
requests>=2.33.0
pandas>=2.1.0
numpy>=1.26.0
tqdm>=4.66.0
python-slugify==8.0.1
phonenumbers==8.13.0
validators==0.22.0

# Database
psycopg2-binary==2.9.9

# API
fastapi>=0.118.0
uvicorn[standard]==0.30.0
asyncpg==0.29.0
pydantic-settings==2.5.0
email-validator>=2.0.0
bcrypt>=4.0.0
httpx>=0.27.0

# Payments
stripe>=8.0.0

# Runtime hardening
# AES-GCM at-rest encryption for webhook signing secrets (api/utils/crypto.py).
cryptography>=43.0.0
# Shared burst/quota rate limiting across workers (api/middleware/rate_limit.py).
# Falls back to in-memory if REDIS_URL is unset; production must run Redis.
redis>=5.0.0

# Testing
pytest>=9.0.3
pytest-asyncio>=0.25.0
httpx==0.27.0
pytest-cov==5.0.0
openpyxl==3.1.5
